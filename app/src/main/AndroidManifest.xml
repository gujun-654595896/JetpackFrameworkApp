<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gujun.jetpack">

    <application
        android:name=".app.JetpackApplication"
        android:allowBackup="true"
        android:icon="@mipmap/app_ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.JetpackFrameworkApp">

        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.NavigationHostActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.activity.StudentListActivity"
            android:screenOrientation="portrait" />

    </application>

</manifest>